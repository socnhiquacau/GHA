apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: youtube-demo
spec:
  selector:
    app: gateway-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9090
      targetPort: 9090
